*******IMPORTANT*******
-------------------------------
To run you will need to create local config file.

Create file called 'config.py'
Copy content from example-config.py and update to your values.


TO RUN:
run using 'python QuipFolderExporter.py'


SUGGESTED UPDATES:
- add command line options.
- convert README to Markdown
- add options for other export types (only Markdown currently)
- add support for different authentication options.

